<% if user_signed_in? %>
    <% if post.favorited_by?(current_user) %> 
        <%=link_to  "", post_favorites_path(post_id: post.id), method: :delete, remote: true, class:"fa fa-heart like-btn"%>
   <%= post.favorites.count %>
    <% else %>
        <%=link_to "", post_favorites_path(post.id), method: :post, remote: true ,class:"fa fa-heart-o fa-border like-btn-unlike"%>
    <%= post.favorites.count %>
    <% end %>
  <% else %>
    <p><span>お気に入り数: </span><%= post.favorites.count %></p>
  <% end %>